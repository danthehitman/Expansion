using UnityEngine;

public class GoBackButtonScript : MonoBehaviour
{
    public GoBackButtonScript button;
    // Start is called before the first frame update
    void Start()
    {
        button btn = button.getcom
    }

    // Update is called once per frame
    void Update()
    {

    }
}
